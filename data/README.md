# Runtime user data (gitignored)

Put your own CSVs, SQLite DB, and snapshots here. Nothing in this folder should be committed except this README and `.gitkeep`.

Bootstrap a demo DB with:

```bash
python scripts/seed_demo.py
```
